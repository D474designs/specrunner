// add x and y
var sum = (x, y) => {
  if (typeof x === 'string') {
  return 'Not a Number';
} else { return x + y;}
};

// multiply y from x
// var multiply = ...
var multiply = (x, y) => {
  return x * y;
};
