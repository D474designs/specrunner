// add x and y
var sum = function (x, y) {
  if (typeof x === 'string') {
  return 'Not a Number';
} else { return x + y;}
};

// multiply y from x
// var multiply = ...
var multiply = function (x, y) {
  return x * y;
};
